- When deduplicating strings or integers inside a loop, assign the values as associative array keys (e.g., `$array[$value] = true;`) and extract them afterward using `array_keys($array)`. This avoids the O(N log N) overhead of `array_values(array_unique($array))` and reduces time complexity to O(N), which is particularly beneficial for large collections like Symfony RouteCollections.
